git init   # 如果还没初始化
git add .
git commit -m "Initial commit - production ready VibeHub"
git branch -M main
git remote add origin https://github.com/你的用户名/vibehub.git   # 先在 GitHub 创建空 repo
git push -u origin main